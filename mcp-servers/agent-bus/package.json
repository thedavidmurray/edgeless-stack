{
  "name": "agent-bus",
  "version": "0.2.0",
  "description": "Inter-session communication for Claude Code agents via a durable message bus",
  "scripts": {
    "hub": "bun run agent-bus-hub.ts",
    "test": "bun run test-bus.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  }
}
